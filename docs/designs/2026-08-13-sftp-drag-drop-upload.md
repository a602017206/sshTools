# SFTP 文件拖放上传设计

## 背景

右侧文件管理器目前只能通过文件选择器上传，桌面用户无法将 Finder 等本地文件直接拖入远程目录。

## 方案

启用 Wails 原生文件拖放，使前端可获得系统拖入文件的绝对路径。文件管理区域注册为唯一的 Wails 拖放目标；接收后复用已有 `UploadFiles` 和传输进度订阅逻辑。仅在已连接的远程 SSH 会话中启用，避免本地终端或断开状态误触发上传。

## 风险

拖放功能依赖 Wails 桌面运行时；浏览器预览环境不提供文件绝对路径，原有“上传文件”选择器继续可用。
